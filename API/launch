# Backup from old project
#python -m uvicorn main:app --host 0.0.0.0 --port 8000 --ssl-keyfile ssl/key.pem --ssl-certfile ssl/cert.pem --loop asyncio --reload
# Command to install uvicorn : pip install uvicorn
# Birth date : 12 june 14:54 Name : Pedro

# Setting env variables
source .env

# Command to launch server
# python -m uvicorn main:app --host 0.0.0.0 --port 32400 --loop asyncio --reload # HTTP
venv/bin/python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 --ssl-keyfile ssl/key.pem --ssl-certfile ssl/cert.pem --loop asyncio --reload # HTTPS
